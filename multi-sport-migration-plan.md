# Миграция на мульти-спорт архитектуру

## Контекст

Изначальная модель `Workout` хранила все данные (включая cycling-специфичные метрики:
`distance_km`, `avg_watts`, `training_stress_score` и т.д.) в одной плоской таблице.
Это работало, пока приложение поддерживало только велоспорт.

Теперь добавляется бег (и в будущем — другие виды спорта), а также переход с `.csv`
на `.fit` файлы. Плоская модель для этого не годится: разные виды спорта имеют
принципиально разные метрики (мощность/каденс для вело, темп/шаги для бега).

## Принятые архитектурные решения

### 1. Модель данных: `Workout` + специфичные для спорта таблицы

- `Workout` — общие поля: `id`, `sport`, `started_at`, `duration`, `source_file_id`, `user_id`.
- `CyclingWorkout` — специфика велоспорта (`distance_km`, `avg_watts`, `normalized_power`, ...),
  связана с `Workout` через `workout_id: int = Field(foreign_key="workout.id", unique=True)`.
  Связь 1:1.
- `RunningWorkout` — по аналогии, для бега (темп, шаги и т.д.). **Ещё не создана.**

**Почему без `Relationship()` в SQLModel:** добавление `Relationship` в `Workout` на каждый
новый вид спорта означало бы раздувание модели `Workout` при каждом новом спорте
(`cycling_workout`, `running_workout`, `swimming_workout`, ...). Вместо этого связь
поднимается вручную в репозитории: узнаём `workout.sport`, затем явным запросом ищем
соответствующую специфичную запись по `workout_id`.

### 2. Как передавать данные из репозитория в сервис

**Не** пытаться "приклеить" `CyclingWorkout` к объекту `Workout` через `workout.cycling_data = ...`
— SQLModel/Pydantic по умолчанию запрещает произвольные атрибуты (`extra='allow'` — это
неявная магия, усложняющая чтение кода).

**Вместо этого** — явный контейнер: репозиторий возвращает пары
`tuple[Workout, CyclingWorkout]` (или список таких пар). Явное лучше неявного.

### 3. Калькуляторы статистики: один класс на вид спорта

`StatsCalculator` сейчас жёстко завязан на cycling-поля. По Single Responsibility —
не городить `if sport == 'cycling' / elif sport == 'running'` внутри одного класса,
а сделать отдельные калькуляторы:

- `CyclingStatsCalculator` — принимает `list[tuple[Workout, CyclingWorkout]]`
- `RunningStatsCalculator` — принимает `list[tuple[Workout, RunningWorkout]]` (будущее)

Каждый калькулятор знает только про свой вид спорта.

### 4. DTO: отдельный на каждый вид спорта

Не один "универсальный" `WorkoutsDTO` с кучей `Optional`-полей на все виды спорта
(это тот же анти-паттерн, из-за которого сейчас идёт рефакторинг `Workout`).

- `CyclingStatsDTO`
- `RunningStatsDTO` (будущее)

### 5. UI / роутер `/statistics`

- Пользователь задаёт приоритетный вид спорта в профиле — он показывается на главной
  вкладке статистики.
- Остальные виды спорта — в отдельных вкладках, либо через фильтр
  (`?sport=cycling`, `?sport=running`).

## План действий (по шагам)

1. ✅ `CyclingWorkout` модель создана (`app/models/training_models.py`)
2. ⬜ Создать `RunningWorkout` модель — **симметрично `CyclingWorkout`**, чтобы сразу
   видеть общий паттерн, но пока не реализовывать бег до конца
3. ⬜ Остановиться на беге, доработать cycling-путь целиком, доказав архитектуру:
   - Репозиторий: метод возвращающий `tuple[Workout, CyclingWorkout]` (или список пар)
     по `user_id` / `period`, без `Relationship`, через явный запрос по `workout_id`
   - `CyclingStatsCalculator` — переписать текущий `StatsCalculator` под новую сигнатуру
     (принимает пары, а не голые `Workout`)
   - `CyclingStatsDTO` — переименовать/адаптировать текущий `WorkoutsDTO`
   - Обновить `parse_csv_to_workout` / `ImportService`, чтобы создавали и `Workout`,
     и `CyclingWorkout` при импорте
   - Обновить тесты и фикстуры (`load_workout` в `conftest.py` использует старые поля —
     нужно адаптировать под новую схему)
4. ⬜ Пересоздать `data/app.db` под новую схему (старая таблица `workout` физически
   несовместима — `NOT NULL constraint failed: workout.moving_time`)
5. ⬜ Когда cycling-путь стабилен и покрыт тестами — по образцу реализовать `running`

## Технические долги, не забыть

- `data/app.db` нужно пересоздать/промигрировать после того как схема `Workout`
  стабилизируется (сейчас рассинхронизирована со старой структурой)
- Переход `.csv` → `.fit` файлы — отдельная задача, после того как схема данных устаканится
- `load_workout` фикстура в `conftest.py` ссылается на несуществующие поля
  (`max_speed`, `total_ascent`, `distance_km`, `moving_time`) — обновить вместе с п.3
